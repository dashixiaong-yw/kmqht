# v2.37 滚动重构回归审查报告

## 审查范围

14 项检查，覆盖 ViewModel、Screen、其他模块、已有功能兼容性。

---

## 审查结果

### 1. ViewModel 侧（5 项）

| # | 检查项 | 结果 |
|:-:|:-------|:----:|
| 1 | `MutableSharedFlow`/`SharedFlow`/`asSharedFlow` import 是否可删除 | ❌ **不可删** — 仍被 `scanSuccessEvent`(L102-103) 和 `scanFailureEvent`(L106-107) 使用，维持原状 ✅ |
| 2 | `_needScroll` 增量只在成功分支触发 | ✅ 仅 L271 `_needScroll.value + 1`，位于 `try` 块末尾，Room 写入之后 |
| 3 | 409 重复扫码不触发 `_needScroll` | ✅ catch 块中仅调用 `syncItemsFromBackend()` + `_duplicateScan.value = true`，不触发滚动 |
| 4 | `_pendingItems` 清理仍在 `finally` 中 | ✅ L281-283 保留，类型 `List<String>` 不变 |
| 5 | `_executeAddItem` 中没有遗漏路径触发滚动 | ✅ 所有成功路径：try 末尾触发；所有失败路径：catch 抛出/处理，不触发；finally：仅清理 pending |

### 2. Screen 侧（6 项）

| # | 检查项 | 结果 |
|:-:|:-------|:----:|
| 6 | 首次进入页面：`orderId` 变化 → LaunchedEffect 执行 → 滚动到顶部 | ✅ |
| 7 | 重新进入同一页面（新 BackStackEntry）：新的 composition → LaunchedEffect 重新执行 → scrollToItem(0) | ✅ |
| 8 | 扫码添加成功：`needScroll` 递增 → LaunchedEffect 重新执行 → 滚动到顶部 | ✅ |
| 9 | 下拉刷新/后台同步：不触发 `needScroll` → LaunchedEffect 不执行 | ✅ |
| 10 | 供应商过滤切换：只影响 `filteredItems`，不触发 `needScroll` → 不滚动 | ✅ |
| 11 | 连续快速扫码：LaunchedEffect 被多次触发，`scrollToItem(0)` 重复执行 — 幂等操作无副作用 | ✅ |

### 3. 其他模块引用（2 项）

| # | 检查项 | 结果 |
|:-:|:-------|:----:|
| 12 | 全局搜索 `scrollToTopEvent` — 代码中是否还有残留 | ✅ **零残留** — 仅在 CHANGELOG.md 有历史记录 |
| 13 | 全局搜索 `_scrollToTopEvent` — 同上 | ✅ **零残留** |

### 4. 已有功能兼容性（1 项）

| # | 检查项 | 结果 |
|:-:|:-------|:----:|
| 14 | 重复扫码 `animateScrollToItem(duplicateIndex)` 是否正常 | ✅ 独立 LaunchedEffect(duplicateScan)，不经过 needScroll，不受影响 |

---

## 结论

**14 项检查全部通过，无回归风险。** 改动不存在跨模块影响，所有 import 变动正确。

| 场景 | 预期行为 | 风险 |
|:-----|:---------|:----:|
| 进入页面 | 视口在顶部 | 无 |
| 扫码添加 | 新商品自动显示在视口 | 无（这是本次修复的目标） |
| 重复扫码 | 滚动到重复行 | 无（独立路径） |
| 下拉刷新 | 当前视口不动 | 无（不触发 needScroll） |
| 供应商过滤 | 列表更新，视口不动 | 无（不触发 needScroll） |
| 连续扫码 | 最后一次添加后滚动到顶部 | 无（幂等操作） |
| 后台 OTA | 不触发 | 无（不经过 needScroll） |
