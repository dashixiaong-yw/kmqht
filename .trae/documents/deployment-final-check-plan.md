# 部署前最终检查计划

> 日期：2026-06-17
> 背景：7次审计修复65个问题，所有代码逻辑、流程模拟、回归验证均通过。本次为部署前最后一站。

---

## 发现

仅1个残留问题：`docker-deploy/backend/` 旧版目录仍存在，与当前扁平部署结构不一致。同步脚本不会清除它。

## 操作

1. 删除 `docker-deploy/backend/` 旧目录
2. 重新运行同步脚本确保部署产物一致
3. 构建APK验证
4. Git提交+推送

## 验证

- `./gradlew assembleDebug` 成功
- 版本号 v1.18
