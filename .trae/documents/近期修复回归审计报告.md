# 4个近期修复回归审计报告

## 审计结果总表

| 修复 | 版本 | 状态 | 问题 |
|:----|:----:|:----:|:-----|
| minSdk 24→23 + ThreadLocal | v1.94 | ✅ 全部正确 | — |
| EncryptedSharedPreferences fallback | v1.95 | ✅ 全部正确 | — |
| URL拼接 `/` + 去掉文字标签 | v1.96 | ✅ 全部正确（5处+PickItemRow） | — |
| Coil ImageLoaderFactory + SSL | v1.97 | ✅ 全部正确（App.kt+6处AsyncImage） | — |
| **SKU图90×90 + 行高保持** | **v1.96 之后** | **❌ 1处缺失** | **L80 padding 未改为4dp** |

---

## 详细检查

### 修复1 (v1.94): minSdk 24→23 + TimeUtils ✅

| 检查项 | 结果 |
|:-------|:----:|
| `minSdk = 23` (build.gradle.kts L37) | ✅ |
| ThreadLocal 使用 `object : ThreadLocal<SimpleDateFormat>()` + `override initialValue()` (API 1+) | ✅ 而非 API 24 的 `withInitial` |
| AGP 自动 v1 签名（minSdk<24 时默认开启） | ✅ 无需显式配置 |

### 修复2 (v1.95): EncryptedSharedPreferences fallback ✅

| 检查项 | 结果 |
|:-------|:----:|
| try-catch 包裹 create (NetworkModule.kt L74-L92) | ✅ |
| `catch (e: Exception)` 不漏异常 | ✅ |
| `File(...).delete()` 兼容 API 23 | ✅ |
| `Log.w` import 存在 | ✅ |
| catch 内重建失败会抛到 Hilt（预期崩溃行为） | ✅ |

### 修复3 (v1.96): URL拼接 `/` + 去掉文字标签 ✅

| 位置 | 当前代码 | 结果 |
|:-----|:---------|:----:|
| ProductViewModel.kt L208 | `"${serverUrl.trimEnd('/')}/${url.imageUrl}"` | ✅ |
| ProductViewModel.kt L211 | `"${serverUrl.trimEnd('/')}/${url.imageUrl}"` | ✅ |
| ProductViewModel.kt L413 | `"${serverUrl.trimEnd('/')}/$imageUrl"` | ✅ |
| PickDetailViewModel.kt L476 | `"${serverUrl.trimEnd('/')}/${url.imageUrl}"` | ✅ |
| PickDetailViewModel.kt L477 | `"${serverUrl.trimEnd('/')}/${url.imageUrl}"` | ✅ |
| PickItemRow 库区图 Box | 无 Text 标签 | ✅ |
| PickItemRow 箱图 Box | 无 Text 标签 | ✅ |

### 修复4 (v1.97): Coil ImageLoaderFactory ✅

| 检查项 | 结果 |
|:-------|:----:|
| `class App : Application(), ImageLoaderFactory` (App.kt L39) | ✅ |
| `newImageLoader()` 使用 `trustAllClient` | ✅ |
| `@Inject @field:Named("trustAll")` 声明正确 | ✅ |
| `CoilModule.kt` 已删除 | ✅ |
| 6 处 AsyncImage 在 ImageLoaderFactory 作用域内 | ✅（全局自动生效） |
| picPath（阿里CDN）使用 trustAll 安全性 | ⚠️ 可接受（内部PDA场景） |

---

## ❌ 缺陷：SKU 图行高未保持（1处遗漏）

当前文件状态：

| 位置 | 预期值 | 实际值 | 差异 |
|:-----|:------:|:------:|:----:|
| L87 `size` | 90×90 | 90×90 | ✅ 已修正 |
| **L80 `vertical`** | **4.dp** | **10.dp** | ❌ **未改成功** |

**根因**：上次修改时 `SearchReplace` 工具虽返回成功，但实际只修改了 L87（size 52→90），L80 的 padding 修改未生效。

**影响**：行高 = 90(图) + 10×2(padding) = **110dp**，而非预期的 98dp。每行多出 12dp。

---

## 修复操作

1. `PickItemRow.kt` L80: `vertical = 10.dp` → **`4.dp`**
2. 验证行高：90 + 4×2 = **98dp** ✅
3. `./gradlew assembleRelease`

## 验证步骤

1. 确认 PickItemRow.kt L80: `vertical = 4.dp`
2. 确认 PickItemRow.kt L87: `size(90, 90)`
3. `./gradlew lint && assembleRelease`
4. 安装 PDA 确认列表滚动行数与调整前一致
5. 确认图片 90×90 显示正常
